# OpenPrints API base URL (no trailing slash). Used by the client for status and API calls.
# Default when running the indexer API locally: http://localhost:8080
PUBLIC_OPENPRINTS_API_URL=http://localhost:8080
